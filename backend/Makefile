# Makefile for FastAPI project

# Run the FastAPI server
run:
	python -m app.main

# Initialize the database
init-db:
	python -m app.db.init_db

# Enter a Python shell with the virtual environment
shell:
	python -i

# Run tests (assuming pytest is installed)
test:
	pytest -v
